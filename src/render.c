#include "game.h"
#include <ncurses.h>

void draw_maze(Maze maze) {
  (void)maze;
}

void update_maze(Maze maze, Position *position, Direction direction) {
  (void)maze;
  (void)position;
  (void)direction;
}
